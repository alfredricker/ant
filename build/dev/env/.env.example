# Dev defaults. Safe to commit — never used outside of local docker.
ENVIRONMENT=dev

# --- postgres ---
POSTGRES_USER=ant
POSTGRES_PASSWORD=ant
POSTGRES_DB=ant
POSTGRES_PORT=5432

# --- app ---
APP_PORT=6353
RUST_LOG=debug,sqlx=warn

# --- nginx (host port the stack is reachable on) ---
NGINX_PORT=8080
